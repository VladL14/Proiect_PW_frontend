/**
 * Production environment.
 *
 * Set apiBaseUrl to the PUBLIC URL of your deployed backend, including the
 * trailing "/api" and NO trailing slash. Example:
 *   apiBaseUrl: 'https://dice-duel-api.onrender.com/api'
 *
 * This value is baked in at build time, so rebuild the frontend whenever the
 * backend URL changes.
 */
export const environment = {
  production: true,
  apiBaseUrl: 'https://dice-duel-api.onrender.com/api'
};
