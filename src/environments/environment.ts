/**
 * Default (development) environment.
 * The production build replaces this file with environment.prod.ts
 * (see the fileReplacements entry in angular.json).
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000/api'
};
